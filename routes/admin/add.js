const Content = require("../../core/classes/utils/content");
/**
 * Handle add request
 * @param {Object} request
 * @param {Object} response
 * @return {void | *}
 * */
const handleAddRequest = (request, response) => {

};

module.exports = handleAddRequest;